<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@yield('title', 'AgroTrace BTC')</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-slate-50 text-slate-900 font-sans flex min-h-screen">

    <!-- SIDEBAR -->
    <aside class="w-64 bg-green-900 text-white flex-shrink-0 hidden md:flex flex-col">
        <a href="{{ url('/') }}" class="p-6 text-xl font-black tracking-tighter border-b border-green-800 hover:text-white block no-underline text-white">
            AGROTRACE <span class="text-orange-400">BTC</span>
        </a>
        <nav class="flex-grow p-4 space-y-2">
            <a href="{{ url('/dashboard') }}" class="flex items-center gap-3 p-3 {{ request()->is('dashboard') ? 'bg-green-800' : 'hover:bg-green-800' }} rounded-xl font-bold no-underline text-white">
                <i class="fa-solid fa-chart-line w-5"></i> Dashboard
            </a>
            <a href="{{ url('/projects') }}" class="flex items-center gap-3 p-3 {{ request()->is('projects') ? 'bg-green-800' : 'hover:bg-green-800' }} rounded-xl transition no-underline text-white">
                <i class="fa-solid fa-leaf w-5"></i> Browse Projects
            </a>
            <a href="{{ url('/verification') }}" class="flex items-center gap-3 p-3 {{ request()->is('verification') ? 'bg-green-800' : 'hover:bg-green-800' }} rounded-xl transition no-underline text-white">
                <i class="fa-solid fa-shield-halved w-5"></i> Live Proofs
            </a>
        </nav>
        <div class="p-6 border-t border-green-800">
            @auth
            <div class="flex items-center gap-3 bg-green-950 p-3 rounded-xl mb-3">
                <div class="w-8 h-8 rounded-full bg-orange-500 flex items-center justify-center font-bold text-xs">
                    {{ strtoupper(substr(Auth::user()->name, 0, 2)) }}
                </div>
                <div class="text-xs">
                    <p class="font-bold text-white m-0">{{ Auth::user()->name }}</p>
                    <p class="text-green-500 m-0">{{ ucfirst(Auth::user()->role) }}</p>
                </div>
            </div>
            <a href="{{ url('/logout') }}" class="btn btn-outline-light btn-sm w-100">Logout</a>
            @else
            <a href="{{ url('/login') }}" class="btn btn-warning w-100 font-bold">Login</a>
            @endauth
        </div>
    </aside>

    <!-- MAIN CONTENT -->
    <main class="flex-grow flex flex-col">
        <!-- Top Mobile Header -->
        <header class="md:hidden bg-green-900 text-white p-4 flex justify-between items-center">
            <a href="{{ url('/') }}" class="text-xl font-black tracking-tighter no-underline text-white">
                AGROTRACE <span class="text-orange-400">BTC</span>
            </a>
            <i class="fa-solid fa-bars text-xl"></i>
        </header>

        <div class="flex-grow overflow-auto">
            @yield('content')
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
